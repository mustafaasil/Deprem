@model List<Deprem>


<table class="table table-striped" id="my-table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Şehir</th>
            <th scope="col">Şiddet</th>
            <th scope="col">Tarih</th>
        </tr>
    </thead>
    <tbody>
        @for (int i = 0; i < ViewBag.IlMaras.Count; i++)
        {
            <tr>
                <td>@(i+1)</td>
                <td>@ViewBag.IlMaras[i]</td>

                @{
                    if (ViewBag.SiddetMaras[i] >= 3.0m && ViewBag.SiddetMaras[i] < 4.0m)
                    {
                        <td class="bg-warning">@ViewBag.SiddetMaras[i]</td>
                    }
                    else if (ViewBag.SiddetMaras[i] >= 4.0m)
                    {

                        <td class="bg-danger">@ViewBag.SiddetMaras[i]</td>
                    }
                    else
                    {
                        <td>@ViewBag.SiddetMaras[i]</td>

                    }
                }
                <td>@ViewBag.SiddetMaras[i]</td>
                <td>@ViewBag.TarihMaras[i]</td>
            </tr>
        }
    </tbody>
</table>

<a asp-action="Index" asp-controller="Home">ANASAYFAYA DÖN</a>