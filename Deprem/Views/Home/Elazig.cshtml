@model List<Deprem>


<table class="table table-striped" id="my-table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Şehir</th>
            <th scope="col">Şiddet</th>
            <th scope="col">Tarih</th>
        </tr>
    </thead>
    <tbody>
        @for (int i = 0; i < ViewBag.IlElazig.Count; i++)
        {
            <tr>
                <td>@(i+1)</td>
                <td>@ViewBag.IlElazig[i]</td>

                @{
                    if (ViewBag.SiddetElazig[i] >= 3.0m && ViewBag.SiddetElazig[i] < 4.0m)
                    {
                        <td class="bg-warning">@ViewBag.SiddetElazig[i]</td>
                    }
                    else if (ViewBag.SiddetElazig[i] >= 4.0m)
                    {

                        <td class="bg-danger">@ViewBag.SiddetElazig[i]</td>
                    }
                    else
                    {
                        <td>@ViewBag.SiddetElazig[i]</td>

                    }
                }

                <td>@ViewBag.TarihElazig[i]</td>
            </tr>
        }
    </tbody>
</table>

<a asp-action="Index" asp-controller="Home">ANASAYFAYA DÖN</a>