@model List<Deprem>


<table class="table table-striped" id="my-table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Şehir</th>
            <th scope="col">Şiddet</th>
            <th scope="col">Tarih</th>
        </tr>
    </thead>
    <tbody>
        @for (int i = 0; i < ViewBag.IlHatay.Count; i++)
        {
            <tr>
                <td>@(i+1)</td>
                <td>@ViewBag.IlHatay[i]</td>

                @{
                    if (ViewBag.SiddetHatay[i] >= 3.0m && ViewBag.SiddetHatay[i] < 4.0m)
                    {
                        <td class="bg-warning">@ViewBag.SiddetHatay[i]</td>
                    }
                    else if (ViewBag.SiddetHatay[i] >= 4.0m)
                    {

                        <td class="bg-danger">@ViewBag.SiddetHatay[i]</td>
                    }
                    else
                    {
                        <td>@ViewBag.SiddetHatay[i]</td>

                    }
                }
                <td>@ViewBag.SiddetHatay[i]</td>
                <td>@ViewBag.TarihHatay[i]</td>
            </tr>
        }
    </tbody>
</table>

<a asp-action="Index" asp-controller="Home">ANASAYFAYA DÖN</a>